import { FormcontrolToLabelPipe } from './formcontrol-to-label.pipe';

describe('FormcontrolToLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new FormcontrolToLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
